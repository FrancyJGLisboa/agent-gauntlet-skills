# What I want

<!-- The user's own words, however rough. A metaphor is a legitimate answer. -->

# Who it is for and what they do with it

<!-- The person, and the decision or task this changes for them. Also who else is
     affected when it is wrong. -->

# Outcomes I refuse to accept

<!-- One sentence per line, in the user's words, each observable by looking at the
     running product. Only sentences the user confirmed. Do not rank, qualify, or
     merge them. This section is the reason the interview exists; if it is thin,
     the interview is not finished. -->

- It must never
- I would stop trusting it if

# Failure shapes that do not apply here

<!-- Shapes the user explicitly ruled out. A rejected shape is information: it stops
     the compiler spending a slice defending against something nobody cares about. -->

# Evidence that exists

<!-- Repository URLs, videos with timestamps, articles, screenshots, a live product.
     Write "none" when there is none; that is a normal answer. -->

# What I have access to

<!-- Data, APIs, files, credentials — and, carrying more weight, what is NOT
     available. An unavailable input stated here is what stops a run building for an
     hour against something that was never going to exist. -->

# Hard constraints

<!-- Must run offline, cannot use a named vendor, licensing limits, files that must
     not change, budget, deadline. "None that I know of" is a real answer. -->

# What I could not answer

<!-- Questions raised in the interview that the user genuinely did not know. Leave
     them here rather than guessing. Each becomes an uncertainty the compiler
     resolves with a bounded experiment and a conservative default. -->
